---
// src/components/WorkflowDetailContent.astro
export interface Props {
  workflow: {
    title: string;
    description: string;
    longDescription?: string;
    features: string[];
    integrations: Array<{name: string; category?: string}>;
    useCases: Array<{title: string; description: string; industry?: string; companySize?: string}>;
    roiMetrics: Array<{metricName: string; metricValue: string; metricLabel: string; metricType: string}>;
    testimonials: Array<{testimonialText: string; authorName: string; authorRole: string; authorCompany: string; rating: number}>;
    nodeCount: number;
  };
}

const { workflow } = Astro.props;
---

<div class="space-y-12">
  <!-- Descripción Detallada -->
  <section>
    <h2 class="text-3xl font-bold text-gray-900 mb-6">Descripción Completa</h2>
    <div class="prose prose-lg max-w-none text-gray-600 leading-relaxed">
      <p class="text-xl mb-6">{workflow.longDescription || workflow.description}</p>
      
      <div class="bg-gradient-to-r from-blue-50 to-purple-50 rounded-2xl p-8 border border-blue-100">
        <h3 class="text-xl font-semibold text-gray-900 mb-4 flex items-center">
          <span class="mr-3 text-2xl">🎯</span>
          ¿Por qué elegir este workflow?
        </h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div class="space-y-3">
            <div class="flex items-center">
              <svg class="w-5 h-5 text-green-500 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <span>Implementación en {workflow.nodeCount} nodos optimizados</span>
            </div>
            <div class="flex items-center">
              <svg class="w-5 h-5 text-green-500 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <span>Documentación completa paso a paso</span>
            </div>
            <div class="flex items-center">
              <svg class="w-5 h-5 text-green-500 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <span>Testing completo antes de entrega</span>
            </div>
          </div>
          <div class="space-y-3">
            <div class="flex items-center">
              <svg class="w-5 h-5 text-green-500 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <span>Soporte post-implementación incluido</span>
            </div>
            <div class="flex items-center">
              <svg class="w-5 h-5 text-green-500 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <span>Escalable según crecimiento del negocio</span>
            </div>
            <div class="flex items-center">
              <svg class="w-5 h-5 text-green-500 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <span>Monitoreo y alertas integradas</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Casos de Uso Específicos -->
  <section>
    <h2 class="text-3xl font-bold text-gray-900 mb-6 flex items-center">
      <span class="mr-3">🏢</span>
      Casos de Uso Específicos
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {workflow.useCases?.map((useCase, index) => (
        <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-6 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 bg-gradient-to-br from-primary-500 to-secondary-500 rounded-xl flex items-center justify-center text-white font-bold text-lg">
              {index + 1}
            </div>
            <div class="ml-4">
              <h3 class="text-xl font-bold text-gray-900">{useCase.title}</h3>
              {useCase.industry && (
                <span class="inline-block px-2 py-1 bg-blue-100 text-blue-800 text-xs font-medium rounded-full mt-1">
                  {useCase.industry}
                </span>
              )}
            </div>
          </div>
          <p class="text-gray-600 leading-relaxed">{useCase.description}</p>
          {useCase.companySize && (
            <div class="mt-4 pt-4 border-t border-gray-100">
              <span class="text-sm text-gray-500">Ideal para: </span>
              <span class="text-sm font-medium text-gray-700">{useCase.companySize}</span>
            </div>
          )}
        </div>
      ))}
    </div>
  </section>

  <!-- ROI y Resultados -->
  <section>
    <h2 class="text-3xl font-bold text-gray-900 mb-6 flex items-center">
      <span class="mr-3">📈</span>
      ROI y Resultados Comprobados
    </h2>
    
    <div class="bg-gradient-to-br from-green-50 to-blue-50 rounded-2xl p-8 border border-green-100 mb-8">
      <h3 class="text-xl font-semibold text-gray-900 mb-6 text-center">
        Resultados promedio de nuestros clientes:
      </h3>
      
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
        {workflow.roiMetrics?.map((metric) => (
          <div class="text-center bg-white rounded-xl p-6 shadow-lg border border-white/50">
            <div class="text-3xl lg:text-4xl font-bold text-green-600 mb-2">
              {metric.metricValue}
            </div>
            <div class="text-sm text-gray-600 leading-tight">
              {metric.metricLabel}
            </div>
            <div class="mt-2">
              {metric.metricType === 'percentage' && (
                <span class="inline-block w-2 h-2 bg-green-500 rounded-full"></span>
              )}
              {metric.metricType === 'currency' && (
                <span class="text-green-600">💰</span>
              )}
              {metric.metricType === 'time' && (
                <span class="text-blue-600">⏱️</span>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>

    <div class="bg-gray-50 rounded-xl p-6">
      <h4 class="font-semibold text-gray-900 mb-3 flex items-center">
        <span class="mr-2">💡</span>
        ¿Cómo calculamos estos resultados?
      </h4>
      <p class="text-gray-600 text-sm leading-relaxed">
        Basamos nuestras métricas en implementaciones reales con más de 200 clientes durante los últimos 2 años. 
        Los resultados pueden variar según el tamaño de la empresa, volumen de datos y nivel de implementación. 
        Ofrecemos una consultoría gratuita para estimar el ROI específico para tu caso.
      </p>
    </div>
  </section>

  <!-- Flujo del Workflow -->
  <section>
    <h2 class="text-3xl font-bold text-gray-900 mb-6 flex items-center">
      <span class="mr-3">🔄</span>
      Cómo Funciona el Workflow
    </h2>
    
    <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
        <div>
          <h3 class="text-xl font-semibold text-gray-900 mb-4">Proceso Automatizado End-to-End</h3>
          <div class="space-y-4">
            <div class="flex items-center">
              <div class="w-8 h-8 bg-primary-500 text-white rounded-full flex items-center justify-center text-sm font-semibold mr-4">1</div>
              <span class="text-gray-700">Configuración inicial y conexión de integraciones</span>
            </div>
            <div class="flex items-center">
              <div class="w-8 h-8 bg-primary-500 text-white rounded-full flex items-center justify-center text-sm font-semibold mr-4">2</div>
              <span class="text-gray-700">Procesamiento automático de datos en tiempo real</span>
            </div>
            <div class="flex items-center">
              <div class="w-8 h-8 bg-primary-500 text-white rounded-full flex items-center justify-center text-sm font-semibold mr-4">3</div>
              <span class="text-gray-700">Ejecución de acciones basadas en reglas inteligentes</span>
            </div>
            <div class="flex items-center">
              <div class="w-8 h-8 bg-primary-500 text-white rounded-full flex items-center justify-center text-sm font-semibold mr-4">4</div>
              <span class="text-gray-700">Monitoreo continuo y optimización automática</span>
            </div>
          </div>
        </div>
        
        <div class="bg-gradient-to-br from-primary-50 to-secondary-50 rounded-xl p-6 text-center">
          <div class="text-4xl mb-4">⚙️</div>
          <h4 class="font-semibold text-gray-900 mb-2">Arquitectura Robusta</h4>
          <p class="text-gray-600 text-sm">
            {workflow.nodeCount} nodos optimizados con manejo de errores, 
            logs detallados y escalabilidad horizontal automática.
          </p>
          <div class="mt-4 pt-4 border-t border-primary-200">
            <span class="text-xs text-gray-500">Monitoreo 24/7 • Backup automático • Alertas inteligentes</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonios -->
  {workflow.testimonials && workflow.testimonials.length > 0 && (
    <section>
      <h2 class="text-3xl font-bold text-gray-900 mb-6 flex items-center">
        <span class="mr-3">💬</span>
        Lo Que Dicen Nuestros Clientes
      </h2>
      
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {workflow.testimonials.map((testimonial) => (
          <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-8 relative">
            <!-- Quote Icon -->
            <div class="absolute top-6 right-6 text-4xl text-yellow-400 opacity-50">"</div>
            
            <!-- Rating -->
            <div class="flex items-center mb-4">
              {Array.from({ length: 5 }).map((_, i) => (
                <svg 
                  class={`w-5 h-5 ${i < testimonial.rating ? 'text-yellow-400' : 'text-gray-300'}`} 
                  fill="currentColor" 
                  viewBox="0 0 20 20"
                >
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                </svg>
              ))}
              <span class="ml-2 text-sm text-gray-500 font-medium">{testimonial.rating}/5</span>
            </div>
            
            <!-- Testimonial Text -->
            <blockquote class="text-gray-700 italic leading-relaxed mb-6">
              "{testimonial.testimonialText}"
            </blockquote>
            
            <!-- Author -->
            <div class="border-t border-gray-100 pt-4">
              <div class="font-semibold text-gray-900">{testimonial.authorName}</div>
              <div class="text-sm text-gray-600">{testimonial.authorRole}</div>
              <div class="text-sm text-primary-600 font-medium">{testimonial.authorCompany}</div>
            </div>
          </div>
        ))}
      </div>
    </section>
  )}

  <!-- FAQ Section -->
  <section>
    <h2 class="text-3xl font-bold text-gray-900 mb-6 flex items-center">
      <span class="mr-3">❓</span>
      Preguntas Frecuentes
    </h2>
    
    <div class="space-y-4">
      <details class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden">
        <summary class="px-6 py-4 cursor-pointer hover:bg-gray-50 transition-colors font-semibold text-gray-900">
          ¿Qué incluye exactamente la implementación?
        </summary>
        <div class="px-6 pb-4 text-gray-600">
          <p>La implementación incluye la configuración completa del workflow, conexión de todas las integraciones, 
          testing exhaustivo, documentación paso a paso, y sesión de training para tu equipo.</p>
        </div>
      </details>
      
      <details class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden">
        <summary class="px-6 py-4 cursor-pointer hover:bg-gray-50 transition-colors font-semibold text-gray-900">
          ¿Qué sucede si necesito modificaciones?
        </summary>
        <div class="px-6 pb-4 text-gray-600">
          <p>Ofrecemos modificaciones menores sin costo durante los primeros 30 días. Para cambios mayores, 
          proporcionamos una cotización transparente basada en la complejidad.</p>
        </div>
      </details>
      
      <details class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden">
        <summary class="px-6 py-4 cursor-pointer hover:bg-gray-50 transition-colors font-semibold text-gray-900">
          ¿Cómo funciona el soporte post-implementación?
        </summary>
        <div class="px-6 pb-4 text-gray-600">
          <p>Incluimos soporte por email y chat durante el periodo especificado. Para soporte extendido, 
          ofrecemos planes de mantenimiento mensual que incluyen monitoreo proactivo y optimizaciones.</p>
        </div>
      </details>
    </div>
  </section>
</div>