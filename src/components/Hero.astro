---
// src/components/Hero.astro
---

<section class="relative gradient-hero min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute inset-0 bg-gradient-to-br from-white/20 to-transparent"></div>
    <svg class="absolute inset-0 w-full h-full" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
          <path d="M 40 0 L 0 0 0 40" fill="none" stroke="white" stroke-width="1" opacity="0.3"/>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#grid)" />
    </svg>
  </div>

  <!-- Floating Elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-20 left-10 w-20 h-20 bg-white/10 rounded-full blur-xl animate-float animate-delay-100"></div>
    <div class="absolute top-40 right-20 w-16 h-16 bg-accent-500/20 rounded-full blur-lg animate-float animate-delay-200"></div>
    <div class="absolute bottom-40 left-20 w-24 h-24 bg-secondary-500/10 rounded-full blur-xl animate-float animate-delay-300"></div>
    <div class="absolute bottom-20 right-10 w-12 h-12 bg-white/15 rounded-full blur-md animate-float animate-delay-400"></div>
  </div>

  <!-- Main Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div class="space-y-8 animate-fade-in">
      <!-- Badge -->
      <div class="inline-flex items-center px-4 py-2 rounded-full glass border border-white/30 text-white/90 text-sm font-medium">
        <span class="mr-2">🚀</span>
        Workflows Profesionales N8N
      </div>

      <!-- Main Heading -->
      <h1 class="text-4xl sm:text-5xl lg:text-7xl font-bold text-white leading-tight">
        <span class="block">Automatiza tu</span>
        <span class="block gradient-text">
          Negocio Completo
        </span>
        <span class="block text-3xl sm:text-4xl lg:text-5xl mt-2 text-white/90">
          en 24-48 horas
        </span>
      </h1>

      <!-- Subtitle -->
      <p class="max-w-3xl mx-auto text-lg sm:text-xl lg:text-2xl text-white/80 leading-relaxed">
        Workflows profesionales de <strong>N8N</strong> listos para implementar. 
        <br class="hidden sm:block">
        Email marketing, e-commerce, CRM, social media y más.
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <button 
          onclick="openCalendlyModal('general')"
          class="btn-primary hover-lift inline-flex items-center px-8 py-4 text-lg font-semibold shadow-xl"
        >
          <span class="mr-2">📞</span>
          Agendar Demo Gratis
        </button>
        
        <button 
          onclick="document.getElementById('catalog-section').scrollIntoView({behavior: 'smooth'})"
          class="glass hover-scale inline-flex items-center px-8 py-4 text-lg font-semibold text-white border border-white/30 rounded-xl transition-all"
        >
          <span class="mr-2">📋</span>
          Ver Catálogo
        </button>
      </div>

      <!-- Stats -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-8 pt-8 max-w-2xl mx-auto">
        <div class="text-center">
          <div class="text-3xl font-bold text-white">50+</div>
          <div class="text-white/70">Workflows</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold text-white">24-48h</div>
          <div class="text-white/70">Implementación</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold text-white">100%</div>
          <div class="text-white/70">Garantía</div>
        </div>
      </div>

      <!-- Social Proof -->
      <div class="pt-8">
        <p class="text-white/60 text-sm mb-4">Empresas que confían en nosotros:</p>
        <div class="flex flex-wrap justify-center items-center gap-6 opacity-70">
          <div class="px-4 py-2 glass rounded-lg">
            <span class="text-white font-medium">TechCorp</span>
          </div>
          <div class="px-4 py-2 glass rounded-lg">
            <span class="text-white font-medium">InnovateHub</span>
          </div>
          <div class="px-4 py-2 glass rounded-lg">
            <span class="text-white font-medium">GrowthCorp</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <button 
      onclick="document.getElementById('catalog-section').scrollIntoView({behavior: 'smooth'})"
      class="p-2 rounded-full glass border border-white/30 text-white hover:bg-white/30 transition-colors"
      aria-label="Scroll to catalog"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </button>
  </div>
</section>